version: 2

models:
  - name: mart_product_store_overview
    description: "Joined mart combining demand, inventory, and pricing summaries at product/store grain."
    columns:
      - name: product_id
        description: "Product identifier"
        tests:
          - not_null
      - name: store_id
        description: "Store identifier"
        tests:
          - not_null